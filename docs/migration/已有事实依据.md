# 已有事实依据

> 状态：2026-09-16 仍在进行中的静态检查点；不是完整代码审计、业务运行、provider/payment 或发布验收报告。历史章节保留原时点。
> 修订日期：2026-09-16；原始记录日期：2026-09-13。当前 Chat 根为 D:/Codex_Program/DeeIX-Chat/main，配套：[修改方案](D:/Codex_Program/DeeIX-Chat/main/docs/migration/修改方案.md)、[执行与验收清单](D:/Codex_Program/DeeIX-Chat/main/docs/migration/执行与验收清单.md)。
> 当前 run 基线以 baseline.ready.json / baseline/repo-summary.json / baseline/versions.json 为准：Chat VERSION=0.4.1，Chat 无可解析 HEAD；Sub2 记录为 0.2.1-p3.5 / 8d841540c372457bb51456d45a5e87713c02a208。B01—B06、C/S 与第 11—15 节保留历史时点；旧目录或旧 HEAD 结论均不得自动转换为当前事实。

## 0. 2026-09-16 当前静态检查点（D0 状态，经 D1 文档校正）

> **读者优先依据本节。** 下文第 1—15 节保留 2026-09-13 至 2026-09-14 的历史事实、旧快照和当时的有限观察；它们不能覆盖本节的当前边界，也不能把历史观察升级成当前证明。

- **当前工作根与证据根：** Chat D:/Codex_Program/DeeIX-Chat/main；artifact root D:/Codex_Program/DeeIX-Chat/.integration-work/20260916-closeout/run-1789515786。D0 对 Sub2 D:/Codex_Program/Personal_Sub2/main 只做证据检查；这不是总体目标的修改边界。当前总体目标已授权必要的 Chat/Sub2 源码、测试源码、增量迁移源码及文档融合与修复，仍在进行。
- **基线元数据：** baseline.ready.json 在 2026-09-16T00:24:32.2414175Z 有效；repo-summary.json 记录 Chat branch main 但无可解析 HEAD、0 个 Git tracked listing、1784 个纳入快照文件，不能宣称 Chat clean、提交或可复现 HEAD。baseline/versions.json 记录 Chat VERSION=0.4.1、Sub2 backend/cmd/server/VERSION=0.2.1-p3.5；版本文件是真实依据，不使用 README 旧发布标签替代。
- **当前验证边界：** 只做 source、format、type、contract、compile 与 Markdown/link/ID/encoding 静态检查。测试源可以存在，但本阶段不执行测试用例或 test runner；不运行应用/server/runtime/browser/services/containers，不连接数据库或执行迁移，不调用 provider/payment，不部署、不 commit/push、不 cleanup。“不执行业务代码/SQL”在这里表示不执行应用或 SQL/迁移，不表示禁止经授权的源代码编辑。
- **当前总体目标与历史 D0 事实：** 2026-09-14 原始文档阶段确实只写文档；E3/E4/E5 随后已在各自授权内修改 Sub2 源码、测试源码或增量 migration source 并通过各自静态检查。D0 文档未修改业务代码；D1 只校正文档边界，不代替各 source owner 的代码融合。
- **WHO TRIGGERS PAYS：** 可信认证的真实 initiator/triggerer 支付该 operation 及所有实际 billable child work；resource/access owner 保持独立，平台 owner 0 不构成 payer。相同 operation 的 retry/recovery/requeue/lease reclaim 保持原 payer；只有明确创建真实新 operation 才能使用新的 actor。浏览器 UID、owner、current worker、sponsor/shared key、route 或 provider 默认值都不能回退选择 payer；本地确定性 CPU/DB/Tika/BM25/cleanup 工作不凭空制造费用。D01—D05 已由用户确认，不再作为待确认问题重复提问。

### 0.1 已取得的静态构件（不是运行或发布通过）

| 交付 | 当前事实 | 仍未证明 |
|---|---|---|
| I3 identity/auth | onboarding/MFA 权威边界、browser-bound one-use state、403 mapping 与 epoch/SSO 语义已有静态 handoff | 测试、浏览器、运行、provider 和完整身份验收 |
| P2 + G1 group priority/readiness | 显式全局组顺序、去重、membership fail-closed 与 readiness 融合已有静态 handoff | 数据库/运行/发布回滚和全链路准入 |
| S1 + G1 trigger/payer core | trusted actor/resource-owner 分离、显式 child execution、root mismatch 与稳定身份/控制路径已有静态 handoff | producer/queue/billing wiring、数据库字段落地、真实 child execution |
| R1 retry/cancel | 观察到 provider effect/handle 后阻断不安全 repost；Chat owner heartbeat 观察 cancel marker | Sub2 跨实例/异步 provider cancel、权威 usage/settlement；accepted 不等于 stopped/free |
| E4/E5/E6a usage outbox | E4 typed durable snapshot、lease/reclaim、UNKNOWN/quarantine、evidence reconciliation 的 core 静态检查通过；E5 已接 durable capture、trusted run、捕获失败 UNKNOWN、worker/lifecycle 与 replay-safe cache invalidation；E6a 已接 authoritative usage query/cache reconciliation | E6b Chat receipt consumer/projection、Sub2 跨实例/异步取消；migration 246 未应用 |
| V0/V1/V2 compile setup | V0 工具链检查完成；V2 提供官方匹配 SQLite 3.45.1 headers，bounded Chat compile exit 0，artifact 未执行 | 这是限定编译证据，仍不是完整代码融合、运行或发布通过 |

### 0.2 当前进行中项目（不得假定完成）

- **Q1：**queue/lease/file-processing trigger metadata 任务由 supervisor 在时限上限中断，未形成最终 handoff；不能仅凭旧 JSONL 或文件缺失把它描述为 unknown/dead。Q2 作为当前 bounded finalizer 已收束其 queue/run/lease metadata，并形成终端静态报告；相关 producer integration 仍在推进。
- **E5/E6a：**E5-durable-capture-static-final.md、E5-handoff.md 与 E6a-handoff.md 已形成；E5 的 Wire、gofmt、go vet ./...、go build ./...、git diff --check 均记录 exit 0，E6a 已形成 authoritative usage query/cache reconciliation 静态结果。E6b Chat receipt consumer/projection 仍未实现，当前不把 F09 说成全部关闭。
- **C1/F1：**C1 主/临时/媒体/MCP 与 F1 文件/逐文件 producer 已各有受限静态终端报告，且 11:56:10 +08:00 检查时对应句柄不再在当前 run 句柄列表中；这只证明限定静态结果，不证明全部 producer 或全功能收束。
- **V2：**已有官方匹配 SQLite 3.45.1 headers、bounded Chat compile exit 0 的终端静态报告；artifact 未执行。它不等于完整代码融合或发布验收。
- **C2/D1：**C2 run/lease fusion 仍有活动句柄；D1 仅校正四份 migration Markdown 与 controller handoff 的当前范围和结论，不修改业务代码、SQL、依赖、CI 或数据库。

### 0.3 当前源码锚点（仅作本轮定点核验，不构成广泛审计）

- Chat backend/internal/ports/llm/subject.go:24-55,57-86,89-142,158-172：TrustedTriggerContext、authenticated root、durable restore、explicit child、legacy owner setter 的语义。
- Chat backend/internal/transport/http/middleware/auth.go:92-103：已验证 actor 进入 trusted execution subject，冲突失败关闭。
- Chat backend/internal/app/sub2_gateway.go:66-112,114-157：显式 global group order readiness、trusted payer gate 与禁止 owner/route 选择 payer 的路径。
- Chat backend/internal/application/conversation/service_message_context.go:526-545 与 generation_stream.go:960-1025：provider-effect retry guard 与 owner heartbeat cancel-marker observation。
- 以上行号只验证当前 main 的少数符号；不替代完整源码审查。

### 0.4 迁移/存储说明

Sub2 current source 中可单独看到 244_chat_model_sync_authority.sql、245_chat_service_settlement_query_indexes.sql 与 THIS-GOAL246：246_chat_execution_usage_outbox.sql；三者在本检查点均未应用。这些是 source artifacts，不是数据库执行证明。Chat 的 additive execution/attribution fields 继续使用 Chat 现有 GORM/schema registration mechanism；不得虚构 Sub2 checksum migration system，不编辑历史 migration，不执行任何 migration 或实际数据库操作。

### 0.5 当前结论

本轮仍是进行中的静态检查点：E5 与 E6a 已形成受限静态代码融合报告，Q2 已形成限定收束报告，C1/F1 已形成受限 producer 终端报告，C2 仍在推进，V2 仅提供官方匹配 headers 与限定 Chat 编译 exit 0。E6b receipt projection、剩余 producer integration、Sub2 取消/对账、managed-Key、global reindex、auxiliary/service-fee、publication recovery/TTL inputs 及最终独立静态融合复核仍开放。不能宣称完整代码融合通过、六项门禁通过、102 项计划用例通过，或任何新的运行/测试/数据库/provider/release acceptance。

## 1. 证据等级与使用规则

| 标记 | 含义 | 能支持什么 |
|---|---|---|
| U | 用户明确决策 | 本次目标、范围和授权边界 |
| B | 本轮主代理的路径、Git、文件存在与校验和核对 | 基线/交付范围；不能证明业务逻辑正确 |
| C/S | 指定 Luna 子智能体已返回的源码静态观察 | 所列文件/符号在审查范围内的事实；不证明运行行为 |
| P35 | 2026-09-14 对当前 P3.5 / Chat 工作区的有限源码点验 | P35-01—P35-06 为前次定价核对；P35-07—P35-13 为本轮角色/分组接点；均不是全仓库、数据库或运行验收 |
| H | 原 Sub2 旧目录的历史审查线索 | 指导重新定位；不证明当前 main 的资金/API 行为 |
| GAP | 未取得或不充分的证据 | 前置核验/实施/验收任务，不能标为已通过 |
| D | 修改方案中的设计建议 | 拟新增或改造的能力，不是现有源码事实 |

事实的适用范围由**目录、快照、审查范围和证据类型**共同决定。文件或路由存在不等于功能可用；有限范围“未发现”不等于证明全仓库不存在。

2026-09-13 原始文档复用了用户指定的 Luna 静态报告及当时的元数据核对。2026-09-14 前次修订增加定价归属、B05 与 P35-01—P35-06；本轮新增 U13—U15、B06、P35-07—P35-13 及 GAP09/GAP10；没有重新开展完整双仓库审查，不把旧 C/S 报告批量升级为新 HEAD 的已审事实，也不把文档修订包装成财务或运行通过。

## 2. 用户已确认事实

| 编号 | 决策 | 来源与对方案的约束 |
|---|---|---|
| U01 | 独立域名、同服务器，不强制两应用直连同一数据库 | 用户首次澄清；域名独立但身份/资金可以通过服务共享 |
| U02 | 使用 Sub2 邮箱密码用户系统，共享永久/临时余额及模型级别权限 | 用户首次澄清；不复制密码或另建第二套资金权威 |
| U03 | Chat 完整后台和功能整体保留 | 用户后续范围确认；不能默认裁剪为基础文本聊天 |
| U04 | 纳入统一登录 SSO | 用户批注 1；不能用普通密码转发替代跨站 SSO 的最终目标 |
| U05 | 一定不能双重扣费 | 用户批注 2；列为阻断门禁并覆盖重试、回调、后台执行 |
| U06 | 接受保留 Chat 后端并增加身份/钱包适配层 | 用户批注 3；是后续代码方向，不改变本轮文档范围 |
| U07 | Sub2 是唯一认证与资金权威 | 用户批注 4；Chat 保留功能入口，但重叠实际操作必须归权威方 |
| U08 | Chat 无正式迁移数据，现有本地数据仅用于测试 | 用户批注 4；不等于允许自动清库或导入测试余额 |
| U09 | 本轮先产出《修改方案》《执行与验收清单》《已有事实依据》 | 2026-09-13 原始文档请求；不执行业务代码/数据库/部署改造 |
| U10 | Sub2 基线为 `D:\Codex_Program\Personal_Sub2\main` | 用户路径确认；本次版本元数据见 B05 |
| U11 | 批准“Chat 管理价格规则，Sub2 按规则结算” | 2026-09-14 用户批注批准；调整原 R05 价格归属，不改变 Sub2 身份、资金和最终准入权威 |
| U12 | 结合 v0.2.1-P3.5 将定价方案写入三份文档 | 2026-09-14 前次请求；仅文档写入，不授权业务源码、数据库或部署改造 |
| U13 | 接受 Chat “上游”增加 Sub2 账号池、多选组、模型同步/上架及路由边界设计 | 本轮明确批准第一点；对应 R10，管理员选组与用户有效权限相交，不自动扩权 |
| U14 | 采用 `user→user`、`admin→admin`、`super_admin→superadmin` 映射 | 本轮明确批准第二点；对应 R11，替代旧设计的一律普通角色/需另一个 Chat 管理员手工提权要求；包括首登可达、旧绑定同步及撤权 |
| U15 | 将本轮全部讨论落实到三份文档并汇报 | 当前请求明确授权文档更新；仍不授权代码、数据库、provider 或部署改造 |

**授权解释：**U13/U14 是分组选择与角色映射的确认；本轮未单独批准“Chat 应用代无权限玩家访问专属池”。继续保留 Sub2 用户有效权限交集，未来若需要应用代授权须独立确认。角色映射许可只针对受信的人的角色，不把 API Key、premium 或邮箱转成管理权限。

## 3. 基线与本轮元数据核对

### B01：2026-09-13 Sub2 审查基线（历史）

- 用户批准目录：`D:\Codex_Program\Personal_Sub2\main`。
- 本轮 `git rev-parse --show-toplevel`：`D:/Codex_Program/Personal_Sub2/main`。
- 本轮 `git rev-parse HEAD`：`cdea2c27ada42701692f77641e779793f1d11b20`。
- 本轮 `git status --porcelain=v1` 无输出。
- 该 HEAD 与 2026-09-13 身份专项报告相同。因此可把该专项 S01—S03 绑定到这个快照，但不能把它扩大为该提交的完整资金/模型审查。
- 原 `D:\Codex_Program\Personal_Sub2\v0.2.4-p1` 在本轮路径澄清时已确认不存在；不再作为方案输入目录。

### B02：2026-09-13 Chat 基线与局限（历史）

- 目录：`D:\Codex_Program\DeeIX-Chat\0.4.1`。
- 本轮读取 `D:\Codex_Program\DeeIX-Chat\0.4.1\VERSION:1`，值为 `0.4.1`。
- 本轮检查 `D:\Codex_Program\DeeIX-Chat\0.4.1\.git` 不存在；前序审查对目录执行 Git 查询返回非仓库。
- 不宣称 Chat 有可验证的 HEAD 或 clean 工作树。版本文件相同和少数文件指纹也不证明整个源码树与旧审查逐字一致。
- C01—C12 保留原报告时点及限制；关键接点另外记录本轮指纹。正式开发前仍需 W00 的完整源码快照。

### B03：2026-09-13 关键源码文件指纹（历史，本次未重算）

以下是本轮主代理实际读取文件元数据、计算的 SHA-256；不是对子系统行为的重新审查，也不是历史版本匹配证明。

| 绝对路径 | 字节 | SHA-256 |
|---|---:|---|
| `D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\infra\persistence\models\user.go` | 16176 | `4713E3B0EB49FAE56ABCD09F254D39E4FD758FBFF72001F60E44993DC8196556` |
| `D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\repository\auth.go` | 5807 | `D4A752E8AF133F07D6509CA0B7685116C7BBDD38A7A63E7EB75DBB85D23CC17C` |
| `D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\application\auth\provider.go` | 52910 | `A14137F9F5AACCE69168C7B85C2BFF3D5EDD7FCE4AE76E69627FD85490E4D61A` |
| `D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\conversation\handler_message_send.go` | 19997 | `4BDBE97A910B07A5654413E923105DF70A5C4DFD685B8B0FC5B3FE4DFEE95C7D` |
| `D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\repository\billing.go` | 8737 | `CE69253D240B485DB254C8B07BD45417C6B38FCD6171D8C8C76615BCFA09E7EC` |
| `D:\Codex_Program\Personal_Sub2\main\backend\ent\schema\auth_identity.go` | 2328 | `499E0B02102D0264A2BB6E07E3A02504B8F946DE717CBC86E744501B8A9FF28B` |
| `D:\Codex_Program\Personal_Sub2\main\backend\internal\handler\auth_oauth_pending_flow.go` | 68167 | `144E523D4E595D446B57B640CAAAF0A396023D2A833C046494A4D44923D33271` |
| `D:\Codex_Program\Personal_Sub2\main\backend\internal\service\auth_service.go` | 68366 | `DDFB5B3A0E580AF886948E35B26610CA80073F8487F540E27394C5E9E322DC34` |

### B04：2026-09-13 文档交付环境（历史）

2026-09-13 13:28（Asia/Shanghai）检查：

- 交付根目录为 `D:\Codex_Program\DeeIX-Chat`，当时有 `0.4.1` 和 `.preview` 目录。
- 三份目标 Markdown 在本轮编写前均不存在。
- 检查的 `D:\AGENTS.md`、`D:\Codex_Program\AGENTS.md`、`D:\Codex_Program\DeeIX-Chat\AGENTS.md` 均未发现文件。
- 本项不证明两个源码树所有深层目录都没有 AGENTS.md。用户在任务中给出的规则仍适用。

### B05：2026-09-14 P3.5 修订基线

- 用户指定主线目录仍为 D:\Codex_Program\Personal_Sub2\main。
- 主代理实际读取 backend/cmd/server/VERSION，原文为 0.2.1-p3.5；用户称 v0.2.1-P3.5。出处：D:\Codex_Program\Personal_Sub2\main\backend\cmd\server\VERSION:1。
- 实际 git rev-parse HEAD 为 8d841540c372457bb51456d45a5e87713c02a208；git status --porcelain=v1 无输出。
- 这不同于 B01 的 cdea2c27 快照。只将本次 P35-01—P35-06 绑定到当前源码，不自动迁移历史 C/S 结论，也未重算 B03 八个历史指纹。
- Chat 仍使用 D:\Codex_Program\DeeIX-Chat\0.4.1。没有完整 Git 基线，不能声明整棵 Chat 工作树 clean。
- 以上是本地版本/源码元数据，不证明生产正在运行该版本、远端标签/镜像最新或发布已验收。

### B06：本轮分组与角色文档修订复核

- 本轮开始重新执行 `git rev-parse HEAD`、读取 `backend/cmd/server/VERSION` 和 `git status --porcelain=v1`，仍为 B05 的 `8d841540c372457bb51456d45a5e87713c02a208`、`0.2.1-p3.5`，工作树无输出。
- 主代理完整阅读三份当前文档；两名默认配置只读代理点验角色/首登及分组/回退的互不重叠范围，主代理沿关键出处复核并独立写入。仅有有限源码静态证据，不把两个专项称为完整权限审计。
- Chat 仍无完整 Git 基线，不能以文档更新或若干文件点验宣称全树 clean；本轮不改业务源码。
- 用户提供的分组管理截图是设计讨论背景，能说明选择器需要展示的组信息，不证明实时账号数量、启停状态、用户授权或生产版本。真实列表与执行状态应经受控服务查询，未连接截图中的部署或数据库。

## 4. 审查来源与适用范围

| 报告 | 日期、模型与范围 | 可复用内容 | 不可扩张的范围 |
|---|---|---|---|
| A1 Chat 初审 | 2026-09-12，`gpt-5.6-luna / max`，Chat 0.4.1 | 架构、路由、会话/模型/账务接点、功能候选及许可声明 | 只读初审，接近时限后收尾；非完整本地资金写入图或运行验证 |
| A2 Sub2 旧目录初审 | 2026-09-12，`gpt-5.6-luna / max`，原 v0.2.4-p1，HEAD `7ada6fa14c9fc8539ab01c18341d304c2cadebcf` | 历史 auth/Gateway/临时余额/模型目录定位线索 | 与当前 main 不同；报告自己明确未读完财务、Key 与模型准入链 |
| A3 身份绑定专项 | 2026-09-13，`gpt-5.6-luna / max`，Chat + Sub2 main，HEAD 同 B01 | 已有身份实体、provider 登录/绑定接点、Sub2 内部身份/会话信息 | 未审余额/钱包/计费；在所查 auth 范围未见完整 IdP，不是全仓库不存在证明 |
| A4 角色与管理员可达点验 | 2026-09-14，本轮默认配置只读代理，B06 工作区 | P35-07—P35-10 的枚举、JIT、身份端口、登录安全检查与撤销接点 | 未运行首登、浏览器或会话测试；不证明映射和防锁出已实现 |
| A5 分组与调度边界点验 | 2026-09-14，本轮默认配置只读代理，B06 工作区 | P35-11—P35-13 的用户组权限、状态、展示、回退/Simple 与端口字段 | 有限路径点验，未穷尽所有协议与调度组合，不证明应用白名单已实现 |

对应子智能体任务标识（用于追溯原报告，不是源码提交）：

- A1：`01a095d3-dd99-7b20-b292-1edc9f1a63d8`。
- A2：`01a095d3-dfda-7791-9b03-f707c74174b6`。
- A3：`01a0991b-985b-7b93-a392-237c3939b922`。
- A4：`01a09f1c-18c8-7892-80ed-958797599bd1`。
- A5：`01a09f1c-1947-7273-a6de-ebb96a8a8720`。

A1/A2 同轮并行，A3 为后续单独专项；没有把未完成的子范围包装成已验收。本文不引用子智能体自称“建议”作为现有实现。

## 5. Chat 静态事实

### C01：它是完整应用，不是纯聊天前端

**来源：**A1；Chat 0.4.1。

- 初审识别为 Go/Gin 后端与 Next.js/React 前端。
- 后端 API 基路径声明、HTTP 路由与前端静态资源托管均有代码接点。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\cmd\server\main.go:11-17`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\server.go:118-149,234-285`。

**支持的判断：**可以保留 Chat 应用后端进行服务适配，而非必须移植为 Sub2 页面。**不支持：**当前双域名已部署、构建成功或所有功能已可用。

### C02：已有可复用的应用用户与外部身份实体

**来源：**A3；关键文件本轮指纹见 B03。

- `User` 相关模型含 `PasswordEnabled`、`PasswordOrigin`。
- 有 Chat 自己的 `UserSession`。
- `UserIdentity` 包含 `ProviderID`、`ProviderSubject`、`UserID`，可表达外部 subject 与 Chat 用户的绑定。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\infra\persistence\models\user.go:96-108,116-153,174-223`。

**支持的判断：**应先复用现有身份实体，而不是默认新建第二张普通映射表。**未证明：**所有需要的唯一索引、并发原子性、Sub2 provider 配置和生命周期联动已完成。

### C03：身份 repository 已有查找和创建接点

**来源：**A3；关键文件本轮指纹见 B03。

`AuthRepository` 已提供以下符号：

- `CreateWithCredentialAndIdentity`
- `GetUserIdentityByProviderSubject`
- `CreateUserIdentity`
- `DeleteUserIdentity`

位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\repository\auth.go:32-81`。

**支持的判断：**有稳定 subject 绑定与首次用户创建的接入位置。方法声明存在不证明并发、事务与解绑安全已经满足新契约。

### C04：provider 登录/绑定流程已有防冲突与无本地密码用户路径

**来源：**A3；关键文件本轮指纹见 B03。

- `CompleteProviderLogin`、`CompleteProviderBind` 有登录与显式绑定流程。
- 专项报告在所读流程观察到 state/PKCE、稳定 subject 提取及邮箱冲突处理。
- 用户创建分支可设置 `PasswordEnabled: false` 与 `PasswordOriginSSOPlaceholder`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\application\auth\provider.go:370-450,515-621,1212-1326`。

**支持的判断：**Sub2 可作为新的可信身份来源接入这些流程。**未证明：**Chat 当前完整符合所选 OIDC 验证、默认角色安全、Sub2 禁用后已有 Chat session 自动撤销。相关要求仍在 W01/W03。

### C05：Chat 有自己的认证与前端会话机制

**来源：**A1。

- Auth 路由含本地登录/refresh，以及注册、重置、2FA、外部身份相关入口。
- 本地 token 模块使用 JWT HS256 的静态线索；不能把另一个应用的 token 自动当作该 token。
- 已审前端 session 模块含内存状态和 BroadcastChannel；这不构成“全仓库任何 token 都绝不持久化”的安全证明。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\auth\router.go:5-20,31-48`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\pkg\token\token.go:11-18,33-49`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\frontend\shared\auth\session.ts:20-27,38-61,82-119`。

**影响：**SSO 要接入本地会话与账号生命周期，不能只更换登录页面地址；普通本地认证入口也必须纳入权威边界。

### C06：聊天流、取消与恢复有既有协议

**来源：**A1；handler 本轮指纹见 B03。

- 路由包含 `POST /conversations/:id/messages/stream` 和 `POST /conversation-runs/:run_id/cancel`，处于 Chat 的 `/api/v1` API 结构中。
- handler 流式内容类型为 `application/x-ndjson`，不是仅凭名称就能等同于 SSE。
- 存在运行恢复/活动运行流相关入口及客户端取消/AbortController 路径。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\conversation\router.go:33-50`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\conversation\handler_message_send.go:240-329`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\frontend\shared\api\conversation.ts:1008-1020`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\frontend\features\chat\hooks\use-chat-message-submit.ts:357-368,551`。

**影响：**保留 Chat 用户协议，对 Gateway 事件/错误/终态做适配。**未证明：**现有取消与恢复已符合统一钱包的实际结算规则。

### C07：Chat 已有真实账务接点，不是只显示价格

**来源：**A1；repository 和 handler 本轮指纹见 B03。

- Schema 中注册账务账户、余额交易、usage/预留相关模型。
- Billing repository 和消息处理器包含使用会话及结算相关接点。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\infra\persistence\schema\schema.go:47-57`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\repository\billing.go:25-38`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\conversation\handler_message_send.go:259-269,341-346`。

**支持的判断：**统一资金必须处理本地预留/结算边界，不能只改 UI 或 API Base URL。**未证明：**当前完整调用图、所有扣款/退款/回调入口、某个开关足以关闭全部本地资金效果。

### C08：Chat 使用自有 schema 与迁移接点

**来源：**A1。

- Schema 注册用户、会话、模型、附件、账务等实体，并有 GORM `AutoMigrate` 路径。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\infra\persistence\schema\schema.go:12-60,119-139`。
- 初审还识别 PostgreSQL/SQLite 适配，但本文不以未列完整出处的适配细节证明与 Sub2 数据结构兼容。

**影响：**同一 PostgreSQL 实例不等于可直接共用 schema；必须隔离表、权限和迁移器。未发现兼容映射的报告不能被扩展为已证明所有表名冲突。

### C09：模型、上游凭证与权限组有本地实现

**来源：**A1。

- 模型目录、upstream/模型管理、权限组与用户/模型关联有路由与实体接点。
- `Upstream` 含 `BaseURL`、`APIKeysEnc`、`HeadersJSON`；存在服务端密钥处理与路由选择代码。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\domain\channel\types.go:32-74`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\channel\router.go:10-13,15-72`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\admin\router.go:28-37`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\application\channel\service_upstream.go:418-462`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\application\channel\service_routing.go:90-101`。

**影响：**不能把一个 Sub2 Key 放到全局 upstream 就宣称已做用户钱包绑定。**未证明：**已有路由支持按可信 Sub2 用户动态发放/选择合法凭证。

### C10：财务与管理功能并非仅一页钱包

**来源：**A1。

- Billing 路由包括账户、价格/计划、订阅、checkout、兑换、使用记录及管理操作接点。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\billing\router.go:12-42`。
- 其他管理入口见 `D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\admin\router.go:5-37`。

**影响：**后台整体保留时必须覆盖支付、套餐、兑换、赠送、调账等重叠操作。路由存在不证明与 Sub2 套餐、临时余额、退款语义等价。

### C11：功能范围覆盖会话之外的多种入口

**来源：**A1，功能级盘点。

- 会话路由含项目/会话管理、消息、分享、导出等接点；文件路由含上传、处理状态、文本内容及下载删除。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\conversation\router.go:5-67`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\conversation\handler_file.go:38-105,263-288,454-477`。
- 初审另外列出 memory、MCP、promptpreset、skill、knowledgebase 等路由作为功能候选，但没有给出它们完整的执行/计费链。
- 图片/视频、RAG、文档处理等在前序报告中属于需专项核验的功能范围；不能从清单名称认定真实 provider 已支持或费用已覆盖。
- 初审指出临时聊天挂在登录态相关路由下；不能自动把“临时”当“匿名”。

**影响：**全功能保留应扩展调用与计费矩阵，不能只对接主聊天。未取得精确执行出处的功能在 AC-F 用例中待核验，不作为已实现/已可用结论。

### C12：独立站点部署接点与许可声明

**来源：**A1。

- 存在 `BrandTitle`、`BrandLogoURL`、`PublicAPIBaseURL`、`PublicWebBaseURL` 配置接点。
- 前端静态目录托管与 HTTP 服务生命周期有实现接点。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\infra\config\config.go:321-341`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\settings\router.go:5-13`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\app\app.go:514-523`。
- 位置：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\transport\http\server.go:234-285`。
- 初审读取 `D:\Codex_Program\DeeIX-Chat\0.4.1\LICENSE:2-4` 和 `D:\Codex_Program\DeeIX-Chat\0.4.1\NOTICE:4-5`，报告仓库声明 Apache License 2.0。

**不支持的结论：**双域名/TLS/跨域已配置成功；所有第三方资产都使用同一许可证；与 Sub2 结合后的发布合规已完成。

## 6. B01 历史 main 快照的已审身份事实

### S01：Sub2 有内部外部身份映射

**来源：**A3；HEAD 绑定 B01，文件指纹 B03。

- `AuthIdentity` 包含 `user_id`、`provider_type`、`provider_key`、`provider_subject`。
- `Indexes()` 对 provider 三元组设唯一约束。
- 位置：`D:\Codex_Program\Personal_Sub2\main\backend\ent\schema\auth_identity.go:34-96`。

这是 Sub2 内部用户与外部身份的映射，不包含 Chat local_user_id，也不是证明 Sub2 可以对外充当 OIDC Provider。

### S02：Sub2 OAuth 身份查找返回 Sub2 自己的用户

**来源：**A3；HEAD 绑定 B01，文件指纹 B03。

- `(*AuthHandler).findOAuthIdentityUser` 按 provider 三元组查询并返回 `record.UserID`。
- 位置：`D:\Codex_Program\Personal_Sub2\main\backend\internal\handler\auth_oauth_pending_flow.go:537-557`。

该函数说明身份归属的内部语义，不能拿它直接替代 Chat 的用户绑定或跨应用授权码交换。

### S03：Sub2 会话状态不能被忽略

**来源：**A3；HEAD 绑定 B01，文件指纹 B03。

- `JWTClaims` 含 `UserID`、`TokenVersion`、`SessionID`。
- 专项所读 `RefreshToken` 路径检查 active、TokenVersion 和 session binding。
- 位置：`D:\Codex_Program\Personal_Sub2\main\backend\internal\service\auth_service.go:59-69,1376-1415`。

**影响：**Chat 的独立 session/refresh 不能永久绕开 Sub2 的撤销与账号状态。**未证明：**跨应用自动撤销、back-channel logout、introspection 或 Chat 所需身份 Provider 已存在。

A3 在 auth/user/session 的有限审查范围内只观察到 Sub2 消费外部身份，没有观察到 discovery/authorize/token/JWKS 等完整 IdP 入口。此结论必须保持为**有限范围未发现**；W01 仍需完整确认或补齐。

## 7. 旧目录事实只作定位线索

以下来自 A2，原目录已不存在、HEAD 与 main 不同。**不得将下列接口、行号或行为直接写成当前 main 已核验事实。**

| 编号 | 历史观察 | 当前应如何使用 |
|---|---|---|
| H01 | 原目录登录、refresh、auth/me 为 JSON/Bearer 相关链路，包含验证码/2FA/会话检查线索 | 在 main 的 auth 路由和服务重新核对；SSO 必须保留当前实际安全策略 |
| H02 | 原目录 Gateway 有 `/v1/messages`、`/v1/responses`、`/v1/chat/completions`，使用 API Key/组相关中间件 | W02 核对当前入口、用户归因和凭证服务；不能假定普通登录 JWT 直接可调用 |
| H03 | 原目录有独立临时余额 grant/consumption、`AvailableSummary`/`ConsumeFEFO` 等线索；初审没有读完扣费事务 | W02 在 main 读通永久/临时余额、退款、缓存和并发规则；不先写死扣减顺序 |
| H04 | 原目录有 Model Plaza、模型价格资源、组/模型准入与相关服务线索 | W02 核对当前目录、可用性、报价与准入的实际关系；不把展示当授权 |

A2 原基线：`D:\Codex_Program\Personal_Sub2\v0.2.4-p1`，HEAD `7ada6fa14c9fc8539ab01c18341d304c2cadebcf`。主理人没有把其 `usage_*`、APIKeyService、临时余额、价格/模型中间件未完成的审查提升为通过。

## 8. 未取得的关键证据与解除条件

| 编号 | 目前缺口 | 影响 | 解除工作包/所需证据 |
|---|---|---|---|
| GAP01 | 当前 main 的完整 IdP 能力、Chat 所选协议校验及跨应用撤销 | 不能承诺只改 Chat 即完成 SSO | W01/W03：当前源码定位、协议契约、重放/冲突/撤销测试 |
| GAP02 | 当前 main 用户级凭证发放与 Gateway 归因、scope/组/Key 生命周期 | 不能使用公共 Key 冒充共享个人钱包 | W02/W05：合法用户凭证、权限边界与两个用户实际账务归因 |
| GAP03 | P35 已有部分价格与费用入账口径证据，但永久/临时余额、套餐、退款、缓存/并发及新策略配额口径未闭环 | 不能宣布钱包兼容或以零售价重写资金规则 | W02/W04：保留资金规则，验证金额分录、配额及故障用例 |
| GAP04 | 当前 Sub2 有事务内结算去重组织（P35-04），但 Chat 全量资金旁路、跨应用执行去重、UNKNOWN/状态查询未闭环 | 账务去重不等于模型 HTTP 重试结果复用，仍不能证明无双重扣费 | W00/W04/W08：写入覆盖、稳定执行/策略关联、故障注入与分录核对 |
| GAP05 | P35 已有部分价格/倍率解析证据；Chat 策略发布、版本冻结、来源隔离、玩家优惠和完整准入链未实现或验收 | 不能把普通 Sub2 计价、目录或单价同步当作新方案完成 | W02/W05：可信发布/确认、单策略结算、应用隔离、价格版本及历史退款用例 |
| GAP06 | 全功能真实执行链，尤其 RAG/辅助执行/MCP/图片视频等协议和费用 | 不能承诺现有全部功能已能经 Sub2 运行和计费 | W00/W06/W08：实际功能清单、协议矩阵、逐项 provider 与收费证据 |
| GAP07 | 当前 main 的迁移保护、双库部署、权限、许可证/第三方义务 | 不能直接共表或称可发布衍生版本 | W00/W09：当前源码和许可核对、隔离迁移/权限与回滚测试 |
| GAP08 | 编译、数据库、双应用、浏览器、真实 provider/支付及发布运行证据 | 只能下静态规划层面的有条件可行结论 | W08/W09：分层实际执行结果，不用静态报告替代 |
| GAP09 | 应用分组目录/白名单及版本、模型同步/上架、最终账号池约束未闭环 | 多选控件或展示目录不能保证回退不越界，也不授予玩家专属组权限 | W02/W05/W07/W08：受控配置、用户交集、全协议/任务/回退/Simple 边界及 AC-G01—AC-G08 |
| GAP10 | 可信角色/权限版本契约、首次/已有绑定同步、管理员可达与专用绑定守卫仍缺失 | 当前 JIT 固定 user，不能保证管理员首登可达，也不能证明外部降权撤销 | W01/W03/W07/W08/W09：三角色映射、先同步再签会话、首登与 SSO-only 切换及 AC-I13—AC-I20 |

这些缺口不妨碍形成有条件的修改方案，但妨碍将方案称为已实现、全部技术细节已证实或可直接生产上线。

## 9. 事实—决策对应

| 方案决策 | 已有依据 | 仍需满足 |
|---|---|---|
| 保留完整 Chat 后端，通过适配层接入 | U03/U06，C01/C06/C10/C11 | GAP02—GAP06，全功能实际验证 |
| 复用既有通用/专用绑定能力 | C02—C04、S01/S02 为历史；P35-08/P35-09 为当前接点 | 核对统一主体、并发唯一与兼容，不重复新建身份表；安全守卫识别专用绑定 |
| Sub2 统一认证并提供 SSO | U04/U07，C04/C05、S03 | GAP01；提供方能力不能由客户端能力推导 |
| 一个资金权威而不是两套余额同步 | U05/U07，C07/C10 | GAP03/GAP04；完整资金写入、幂等和故障核对 |
| 保留 UI/管理入口并按对象划分权威 | U03/U06/U07/U11，C09/C10 | Chat 管理零售策略，Sub2 执行身份/资金操作；套餐/支付仍需映射 |
| Chat 定价、Sub2 唯一结算 | U11/U12，P35-01—P35-06 仅作改造接点 | 新应用策略契约、可信用量、版本冻结及跨应用幂等/隔离均待实施验收 |
| 上游选择 Sub2 账号池和模型 | U13，P35-11—P35-13 | GAP09；组白名单和用户权限交集，选组/模型上架/回退边界分别验证 |
| 可信三角色映射与管理员不被锁出 | U14，P35-07—P35-10 | GAP10；首次及已有绑定正确角色、撤权和实际管理入口验证，禁止以默认 admin 或删守卫绕过 |
| 数据隔离而非直接共写表 | U01，C08 | 当前 main 迁移/权限、发布/回滚核验 |
| 不进行真实数据迁移或自动清库 | U08 | 隔离测试账号，不把测试金额导入真实钱包 |

## 10. 不得写成“现有事实”的设计项

以下均属于配套方案 D，不应被后续开发者从文档反向引用为已有实现：

- Sub2 已完整提供 OIDC discovery/authorize/token/JWKS 或跨站 logout。
- 已存在一个能安全代理任意 Chat 用户的统一钱包接口。
- P3.5 已具备 Chat 价格策略发布、确认、冻结和专属结算接口，或可直接信任浏览器提交价格/金额。
- 登录 JWT 可以直接替代 Gateway API Key，或一个静态 Key 可以正确归因所有用户。
- 当前 Gateway 已对任意客户端 UUID 实施执行幂等和账务去重。
- 一个配置开关已能关闭所有 Chat 本地资金写入。
- 当前 main 临时余额扣减、退款、过期与套餐规则已经被本轮读通。
- 文本 Gateway 自动支持 Chat 全部图片/视频、embedding 和外部工具费用。
- Chat/Sub2 全部支付、兑换、套餐及细粒度管理权限可以原样一一映射；本轮批准的三角色映射不是“已经完成”的事实，也不扩张 Sub2 财务权限。
- 已有 Chat 应用组白名单可约束所有 fallback/Simple/异步路径，或勾选分组等于授予无权限玩家调用权。
- 当前 Sub2 管理员首登已经映射成功、外部降权自动撤销，或现有 SSO-only 守卫已经识别 Sub2 专用绑定。
- 两个项目可安全指向同一 schema，或当前许可证/发布义务已全部满足。

## 11. 2026-09-13 验证边界与交付结论（历史）

### 已有证据

- 用户确认的目标、SSO、唯一资金权威、无正式迁移与本轮文档边界。
- 前序指定 Luna 静态报告中的 Chat 接点和 main 身份接点。
- 本轮源目录、Sub2 HEAD/工作树、Chat VERSION、关键文件存在与 SHA-256。
- 三份文档的结构与交付范围检查属于文档层证据，检查结果以主代理实际命令输出为准。

### 没有执行

- 本轮没有新一轮完整双仓库代码审计或 main 资金专项通过结论。
- 没有业务代码、SQL、依赖或部署配置修改。
- 没有构建、类型检查、lint、单元/集成测试。
- 没有数据库连接、迁移执行、资金操作、真实 provider/支付、浏览器或部署验证。
- 没有提交、推送、打标签、发布或删除本地测试数据。

**可以得出的最终结论：**推荐架构有明确的静态接入基础，且符合用户确认的目标；可以据此开展经授权的契约设计和代码实施。**不能得出的结论：**两项目已经兼容、完整 SSO/钱包适配已存在、双重扣费风险已经被运行测试排除、全部功能可以直接上线。
## 12. 2026-09-13 文档静态检查记录（历史）

2026-09-13，主代理对三份交付文件执行结构和元数据检查，记录如下：

| 检查 | 结果与范围 |
|---|---|
| 文件/编码/标题 | 三份目标文件存在、非空、UTF-8 无 BOM，标题与文件名匹配 |
| Markdown 基本结构 | 代码围栏成对；无遗留“编制中/等待补齐”起草占位段 |
| 需求与工作包追踪 | 9 个 R 需求、10 个 W 工作包，方案与清单一一对应 |
| 用例与门禁 | 67 个唯一 AC 验收用例、6 个核心阻断门禁；都是后续计划，不是已执行结果 |
| 事实和缺口引用 | 15 个 C/S 源码观察编号与 8 个 GAP 编号，计划中的引用可定位 |
| 配套文档链接 | 指向三份实际绝对路径的文件链接存在 |
| 源码出处的结构 | 32 处带行号引用、27 个唯一源码文件，路径存在且行号未越界；未重审每句代码语义 |
| 关键指纹 | B03 的 8 个关键源码文件与记录的大小/SHA-256 一致；不代表 Chat 全源码树一致 |
| Sub2 基线 | HEAD 仍为 B01，工作树无输出；这是 Git 元数据证据，不是运行验证 |

本轮写入命令只针对三份 Markdown。Chat 无完整 Git 基线，因此不把少数文件指纹或文档检查冒充整棵工作树 clean 证明；本轮没有执行会修改业务代码的命令。追加本节后，交付前再次检查文件结构、关联和源文件指纹，不扩展到未授权的代码/数据库/运行验证。
## 13. 2026-09-14 当前 P3.5 有限静态核对

P35-01—P35-06 保留前次定价修订、B05 时点：当时 Sol（high）读取后超时未写入，由主代理接手。P35-07—P35-13 为本轮 A4/A5 与主代理点验，绑定 B06；两次修订均不是代码或运行验收。

### P35-01：模型单价存在分组、渠道与基础价层次

ModelPricingResolver.Resolve 优先匹配分组价格卡，未命中再查渠道与基础价格。出处：D:\Codex_Program\Personal_Sub2\main\backend\internal\service\model_pricing_resolver.go:74-116。相同平台/模型不意味着不同 group_id 使用同一张价格卡；这是原 Sub2 计价，不是新 Chat 专属策略。

### P35-02：用户倍率优先覆盖，动态因素再组合

EntitlementService.ResolveGroupRate 顺序为用户专属覆盖、权益默认、分组默认；动态组合为 static × peak × dynamic。出处：D:\Codex_Program\Personal_Sub2\main\backend\internal\service\entitlements.go:198-211；D:\Codex_Program\Personal_Sub2\main\backend\internal\service\user_group_rate_resolver.go:136-154；D:\Codex_Program\Personal_Sub2\main\backend\internal\service\dynamic_rate.go:254-267。不能把三种静态来源机械相乘，也不能将其自动叠加到 Chat 新零售策略。

### P35-03：倍率前金额、实扣金额与配额口径不同

TotalCost 汇总 token 等费用，ActualCost = TotalCost × rateMultiplier。账务命令将 ActualCost 用于钱包或订阅分支，以及适用的 Key 配额/限流累计；账号配额另用 TotalCost × AccountRateMultiplier。出处：D:\Codex_Program\Personal_Sub2\main\backend\internal\service\billing_service.go:1454-1456；D:\Codex_Program\Personal_Sub2\main\backend\internal\service\gateway_usage_billing.go:319-337。这些字段不证明真实采购成本，配额累计不等于另扣一次钱包。

### P35-04：已有事务内结算去重组织，但未证明跨应用执行幂等

usageBillingRepository.Apply 在同一事务组织 usage 写入、去重认领、资金/配额效果与提交。出处：D:\Codex_Program\Personal_Sub2\main\backend\internal\repository\usage_billing_repo.go:41-72。普通请求 ID 中间件在未预置有效上下文 ID 时生成新 UUID，见 D:\Codex_Program\Personal_Sub2\main\backend\internal\server\middleware\client_request_id.go:39。因此不能承诺相同客户端 HTTP 重试不会再次执行模型；跨应用状态查询、UNKNOWN 与核对仍属于 GAP04。

### P35-05：Chat 已有模型价格及玩家组倍率接点

Chat 按平台模型名称查询价格；用户与模型适用权限组交集中的倍率取最小值。出处：D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\application\billing\service.go:3359-3368；D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\infra\persistence\postgres\channel\permission_group.go:751-788。这是可复用的本地规则接点，不是已实现价格发布、冻结或 Sub2 扣款。

### P35-06：当前 Sub2 模式仍阻断旧派发和价格读写

sub2BlockedLLMGateway.Generate/ListModels 返回 ErrExecutionDispatchUnavailable；sub2BillingRepository.GetModelPricing/UpsertModelPricing 返回 blocked。出处：D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\app\sub2_authority.go:25-35,157-164。新方案需要后续改造，不能声称开启现有配置即可完成；也不能为恢复价格管理而恢复本地扣款。

### P35-07：两边角色枚举不同，但已批准映射可明确表达

Sub2 `RoleUser/RoleAdmin/RoleSuperAdmin` 为 `user/admin/super_admin`，Chat 为 `user/admin/superadmin`，Chat 的 `IsAdminRole` 接受后两类。出处：`D:\Codex_Program\Personal_Sub2\main\backend\internal\domain\constants.go:13-18`；`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\domain\user\types.go:5-15`。这支持 U14 的显式映射设计，不证明同步已实现。

### P35-08：Sub2 专用 JIT 固定普通角色，身份端口未承载角色

`resolveSub2User` 已有绑定分支更新状态/epoch 等后返回原用户；新建分支仍为 `Role: domainuser.RoleUser`。`IdentityRef`、`IdentityStatusResponse`、`SubjectExchangeResponse` 无权威角色字段。出处：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\application\auth\sub2.go:243-292`；`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\ports\sub2\port.go:10-54`。角色同步需要改造身份契约及首次/已有绑定两条路径，不能只改 provider 全局默认角色。

### P35-09：关闭本地登录的守卫只识别通用外部身份绑定

`applyAuthSettingDependencies` 在邮箱/用户名登录均关闭时要求第三方登录启用并通过 `HasActiveSuperAdminIdentity`。后者查询 `model.UserIdentity`/`identity_user_links`，只计入 active `superadmin`，未检查 Sub2 专用绑定或实际登录成功。出处：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\application\settings\service.go:541-562`；`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\infra\persistence\postgres\user\repository.go:1376-1386`。不能以存在一个 admin 行或只改 JIT 来宣称首次管理员可达。

### P35-10：本地会话撤销已有接点，外部角色撤销尚未闭环

`PatchUserByAdmin` 在本地改角色或改为非 active 时调用全会话撤销；`Refresh` 复核外部身份，`ValidateAccessSession` 检查撤销/过期。`CheckExternalIdentity` 当前只核验绑定、状态和 epoch，没有外部角色同步。出处：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\application\admin\service.go:964-995`；`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\application\auth\service.go:1162-1179,1305-1340`；`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\application\auth\sub2.go:204-240`。不能假定 Sub2 角色变化必然推动现有 epoch 或撤销链。

### P35-11：用户组权限、组状态和模型展示是不同边界

`User.CanBindGroup` 对未限制公开组的用户放行非专属组；专属组及受限用户的公开组需在 `AllowedGroups` 内。组状态检查拒绝停用/删除；订阅组有独立准入路径，不能把此函数解释成全部订阅鉴权。`models_list_config` 明确“仅影响模型列表响应，不影响调度”。出处：`D:\Codex_Program\Personal_Sub2\main\backend\internal\service\user.go:84-99`；`D:\Codex_Program\Personal_Sub2\main\backend\internal\server\middleware\api_key_auth.go:441-463`；`D:\Codex_Program\Personal_Sub2\main\backend\ent\schema\group.go:265-268`。选组与上架模型不自动改变这些权限。

### P35-12：回退可换组，Simple 查询不按组过滤

`resolveGatewayGroup` 沿 `FallbackGroupID` 切换组；`GatewayHandler.Messages` 的特定错误兜底克隆目标组 APIKey 后重试。`listSchedulableAccounts` 在 Simple 分支按指定平台查账号、不按组过滤；普通有组分支按组与平台查询，无账号不回退全平台。出处：`D:\Codex_Program\Personal_Sub2\main\backend\internal\service\gateway_scheduling.go:895-920,1059-1067`；`D:\Codex_Program\Personal_Sub2\main\backend\internal\handler\gateway_handler.go:1055-1088`。所读路径不能证明已有 Chat 白名单校验；不据此断言全部调用链没有鉴权，也未读取部署模式。新增白名单必须约束最终路径而非只筛初始组。

### P35-13：目录/准入端口没有显式应用组白名单上下文

Chat `ModelCatalogRequest` 有 Identity/Application；`ModelAdmissionRequest/Response` 有模型、摘要和准入结果，但未定义允许组集合、组策略版本或最终执行组字段。出处：`D:\Codex_Program\DeeIX-Chat\0.4.1\backend\internal\ports\sub2\port.go:74-118`。有限点验未证明应用级多选组已经接通；具体 DTO/路由由 W02 设计，不能从已有 Application 字段倒推出授权能力。

## 14. 前次定价修订的决策与验证记录（历史）

- U11 调整的是 Chat 应用零售价，不是 Sub2 身份、准入或钱包所有权；普通 Sub2 API 定价不因本次方案变化。
- 三份文档正文同步修订，不只末尾附加例外。执行清单新增 AC-P08—P14 七项定价契约用例；旧第 12 节的 67 个用例是历史统计，不是修订后统计或执行结果。
- 当前运行环境的分组和实际价格数据未读取；没有数据库连接、构建/业务测试、真实 provider/支付、部署或发布验证。
- 本次仅写三份指定 Markdown；结构、引用、唯一用例 ID 与修订一致性由主代理最终命令检查。历史哈希和旧报告不充当新版本完整验证。
### 14.1 前次定价修订的文档静态检查记录（历史）

2026-09-14 主代理执行文档专用检查并在最后修订后复核：

| 检查 | 结果及边界 |
|---|---|
| 文件、UTF-8 与标题 | 三份文件已更新，编码可严格解码，标题与文件名一致 |
| Markdown 围栏、表格和配套链接 | 围栏成对、表格列数一致；7 处配套本地 Markdown 链接可定位 |
| 需求与工作包 | 方案和清单均保留 R01—R09、W00—W09 |
| 用例定义 | 74 个唯一 AC 定义，含新增 AC-P08—P14；只是计划完整性，不是业务测试通过 |
| 定价归属复核 | Chat 规则管理/受控发布、Sub2 可信用量/唯一结算、非 Chat 来源隔离及历史证据边界已核对 |
| Sub2 元数据 | HEAD 与 VERSION 仍为 B05，Git 工作树无输出；不是数据库或运行验证 |

本次未运行构建、业务单元/集成测试、数据库、真实 provider/支付或部署验收。Chat 无完整 Git 基线，不以文档检查声称全源码树 clean。

## 15. 本轮分组选择与角色映射的修订结果

- U13/U14 已落实为 R10/R11；方案新增分组/模型选择、最终调度约束、三角色映射和 SSO-only 管理员可达要求，不再保留“一律普通角色”的现行设计约束。
- 原 R05 的 Chat 零售策略/Sub2 唯一扣款保持不变；分组选择不叠加旧 Sub2 零售倍率，管理员角色也不增加钱包或分组越权能力。
- 本轮新增 GAP09/GAP10，更新 W01/W02/W03/W05/W07/W08/W09 的职责及依赖，新增 AC-I13—AC-I20、AC-G01—AC-G08。原 74 项加 16 项，共 90 项，均为待执行业务用例。
- 本轮仅修订三份 Markdown；当前工作区的有限源码缺口如实记录。未执行编译、业务单元/集成测试、数据库、provider、浏览器或发布验证；文档静态检查结果见本节后附记录。
### 15.1 本轮文档静态检查

主代理完成正文修订与旧约束冲突复核后，执行文档专用检查；追加本记录后再次执行同一检查：

| 检查项 | 结果与证据范围 |
|---|---|
| 三份文件、UTF-8、标题、围栏与表格 | 通过；无 BOM，表格列数一致，围栏成对 |
| 配套文件链接 | 7 处链接可定位 |
| 需求/工作包/门禁 | R01—R11 共 11 项、W00—W09 共 10 包、6 个核心门禁，方案与清单定义一致 |
| 决策/缺口/静态观察 | U01—U15、GAP01—GAP10、P35-01—P35-13 定义唯一；新增引用可解析 |
| 验收用例 | 90 个唯一 AC 定义，新增角色 8 项和分组 8 项；这些是待执行计划，不是业务测试通过 |
| 新源码定位 | P35-07—P35-13 中 15 处文件/行号引用可定位且未越界；语义结论限于所列小范围点验 |
| 原约束保留 | R04/R05/R06/R08/R09、方案第 5.1 节和第 10.2 节与修订前一致，保留资金权威、定价、防重复、数据和文档授权边界 |
| 基线 | Sub2 HEAD / VERSION 仍为 B05/B06，Git 工作树无输出；不代表数据库或生产运行验证 |

该节属于 2026-09-14 历史文档阶段记录：当时工作区写入仅三份指定 Markdown，未运行构建、业务测试、数据库、provider、浏览器、部署、提交或推送。它不限制当前总体代码融合授权，也不把当前 E3/E4/E5 静态证据归入 D0 文档写入。
