{
  "name": "@deeix/api",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "build": "make build",
    "check": "make check-version && make lint",
    "clean": "rm -rf ../.cache/deeix-chat",
    "dev": "make run",
    "test": "make test"
  }
}
